# Uncomment the next line to define a global platform for your project

target 'DevSettingsExample' do
  use_frameworks!
  # Pods for DevSettingsExample
    pod 'DeveloperSettings', :path => '../DeveloperSettings'

end
